# Ambiente local de desenvolvimento (backend)
DJANGO_DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
FRONTEND_ORIGINS=http://localhost:5173
CSRF_TRUSTED_ORIGINS=http://localhost:5173
# FRONTEND_BASE_URL usado nos links de verificação de e-mail
# FRONTEND_BASE_URL=http://localhost:5173
# Em produção, defina FRONTEND_BASE_URL no ambiente do provedor (e.g. Render)

# Permite leitura pública (sem JWT) dos endpoints GET durante o desenvolvimento
PUBLIC_READ=true

# SMTP local (desativado para não depender de provedor externo)
# EMAIL_HOST=smtp.zoho.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=seu-email@dominio.com
# EMAIL_HOST_PASSWORD=app-password
# EMAIL_USE_TLS=true
# DEFAULT_FROM_EMAIL=seu-email@dominio.com
